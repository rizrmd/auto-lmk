# Architecture Documentation - Auto LMK Platform

## 🏗️ System Architecture

### High-Level Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                     CLIENT LAYER                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   Browser    │  │  WhatsApp    │  │  Mobile Web  │       │
│  │   (HTMX)     │  │   Client     │  │   Browser    │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
└─────────┼──────────────────┼──────────────────┼───────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌──────────────────────────────────────────────────────────────┐
│                  NGINX REVERSE PROXY                          │
│         (SSL Termination, Load Balancing, Static Files)       │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER                          │
│  ┌────────────────────────────────────────────────────────┐  │
│  │           Chi Router + Middleware Stack                 │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │  │
│  │  │ Tenant   │  │  Logger  │  │   CORS   │             │  │
│  │  │Extractor │  │          │  │          │             │  │
│  │  └──────────┘  └──────────┘  └──────────┘             │  │
│  └────────────────────────┬───────────────────────────────┘  │
│                           │                                   │
│  ┌────────────────────────┴───────────────────────────────┐  │
│  │                   HANDLERS                              │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐     │  │
│  │  │ Tenant  │ │   Car   │ │  Sales  │ │  Lead   │     │  │
│  │  │ Handler │ │ Handler │ │ Handler │ │ Handler │     │  │
│  │  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘     │  │
│  └───────┼───────────┼───────────┼───────────┼───────────┘  │
│          │           │           │           │               │
│  ┌───────┴───────────┴───────────┴───────────┴───────────┐  │
│  │                    SERVICES                             │  │
│  │  ┌──────────────┐  ┌──────────────┐                   │  │
│  │  │ CarService   │  │ WhatsApp     │                   │  │
│  │  │              │  │ Service      │                   │  │
│  │  └──────┬───────┘  └──────┬───────┘                   │  │
│  └─────────┼──────────────────┼───────────────────────────┘  │
│            │                  │                               │
│  ┌─────────┴──────────────────┴───────────────────────────┐  │
│  │                  REPOSITORIES                           │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐         │  │
│  │  │Tenant  │ │  Car   │ │ Sales  │ │ Lead   │         │  │
│  │  │Repo    │ │ Repo   │ │ Repo   │ │ Repo   │         │  │
│  │  └────┬───┘ └────┬───┘ └────┬───┘ └────┬───┘         │  │
│  └───────┼──────────┼──────────┼──────────┼──────────────┘  │
└──────────┼──────────┼──────────┼──────────┼─────────────────┘
           │          │          │          │
           └──────────┴──────────┴──────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────────────┐
│                     DATA LAYER                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              PostgreSQL 15                              │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐              │  │
│  │  │ tenants  │ │   cars   │ │  sales   │              │  │
│  │  │  users   │ │ messages │ │  leads   │              │  │
│  │  └──────────┘ └──────────┘ └──────────┘              │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                  EXTERNAL INTEGRATIONS                        │
│  ┌──────────────┐  ┌──────────────┐                         │
│  │  WhatsApp    │  │     LLM      │                         │
│  │ (Whatsmeow)  │  │   Provider   │                         │
│  │              │  │(OpenAI/Claude)│                         │
│  └──────────────┘  └──────────────┘                         │
└──────────────────────────────────────────────────────────────┘
```

## 🔐 Security Architecture

### Multi-Tenant Isolation

```
Request Flow:

1. Browser Request
   ├─> Domain: showroom-jaya.localhost
   │
2. Nginx
   ├─> Forward Host header
   │
3. Middleware: TenantExtractor
   ├─> Extract domain from Host header
   ├─> Query: SELECT id FROM tenants WHERE domain = ?
   ├─> Add tenant_id to Context
   │
4. Handler
   ├─> Extract tenant_id from Context
   ├─> Call Repository
   │
5. Repository
   ├─> ALL queries include: WHERE tenant_id = ?
   ├─> Data isolation enforced at DB level
   │
6. Response
   └─> Only tenant's data returned
```

### Authentication Flow (When Implemented)

```
Login:
User → /api/login → Validate credentials → Generate JWT → Return token

Authenticated Request:
User → Header: Authorization: Bearer <token> → Middleware validates JWT
     → Extract user_id → Add to Context → Handler uses user_id
```

## 📊 Data Flow

### Car Search via WhatsApp

```
1. User sends: "Ada Toyota budget 200 juta?"
   │
2. WhatsApp → Whatsmeow → WhatsAppService.ProcessIncomingMessage()
   │
3. Check if sales: SalesRepo.IsSales(phone)
   │
4. Create/Get conversation: ConversationRepo.GetOrCreate()
   │
5. Store message: ConversationRepo.AddMessage()
   │
6. Process with LLM: Bot.ProcessMessage()
   ├─> Build system prompt (sales vs customer)
   ├─> Add conversation history
   ├─> Call LLM with functions
   │   ├─> LLM decides: call searchCars function
   │   └─> Arguments: {brand: "Toyota", max_price: 200000000}
   │
7. Execute function: WhatsAppService.ExecuteBotFunction()
   ├─> CarService.SearchCarsForBot()
   └─> CarRepo.List(ctx, filters)
       └─> SELECT * FROM cars WHERE tenant_id = ? AND brand = ? AND price <= ?
   │
8. LLM formats response in Bahasa Indonesia
   │
9. Store bot response: ConversationRepo.AddMessage()
   │
10. Send via WhatsApp: WhatsAppClient.SendMessage()
    │
11. User receives response
```

## 🗄️ Database Schema

### Entity Relationship Diagram

```
┌─────────────┐
│   tenants   │
│─────────────│
│ id (PK)     │◄──────┐
│ domain      │       │
│ name        │       │
│ whatsapp_   │       │
│   number    │       │
└─────────────┘       │
                      │ tenant_id (FK)
┌─────────────┐       │
│    users    │       │
│─────────────│       │
│ id (PK)     │       │
│ tenant_id───┼───────┤
│ email       │       │
│ password_   │       │
│   hash      │       │
└─────────────┘       │
                      │
┌─────────────┐       │
│    sales    │       │
│─────────────│       │
│ id (PK)     │       │
│ tenant_id───┼───────┤
│ phone_      │       │
│   number    │       │
│ name        │       │
└─────────────┘       │
                      │
┌─────────────┐       │
│    cars     │       │
│─────────────│       │
│ id (PK)     │       │
│ tenant_id───┼───────┤
│ brand       │       │
│ model       │       │
│ year        │       │
│ price       │       │
│ status      │       │
└──────┬──────┘       │
       │              │
       │ car_id (FK)  │
       │              │
┌──────▼──────┐       │
│ car_photos  │       │
│─────────────│       │
│ id (PK)     │       │
│ car_id (FK) │       │
│ file_path   │       │
└─────────────┘       │
                      │
┌─────────────┐       │
│car_specs    │       │
│─────────────│       │
│ id (PK)     │       │
│ car_id (FK) │       │
│ key         │       │
│ value       │       │
└─────────────┘       │
                      │
┌─────────────┐       │
│conversations│       │
│─────────────│       │
│ id (PK)     │       │
│ tenant_id───┼───────┤
│ sender_     │       │
│   phone     │       │
│ is_sales    │       │
└──────┬──────┘       │
       │              │
       │ conversation_│
       │   _id (FK)   │
┌──────▼──────┐       │
│  messages   │       │
│─────────────│       │
│ id (PK)     │       │
│ conversation│       │
│   _id (FK)  │       │
│ sender_phone│       │
│ message_text│       │
│ direction   │       │
└─────────────┘       │
                      │
┌─────────────┐       │
│    leads    │       │
│─────────────│       │
│ id (PK)     │       │
│ tenant_id───┼───────┘
│ phone_      │
│   number    │
│ interested_ │
│   car_id    │
│ status      │
└─────────────┘
```

## 🚀 Deployment Architecture

### Production Setup

```
┌──────────────────────────────────────────┐
│         Internet / Public Network         │
└─────────────────┬────────────────────────┘
                  │
                  ▼
┌──────────────────────────────────────────┐
│          Firewall / Security Group        │
│       (Ports: 80, 443, 22)                │
└─────────────────┬────────────────────────┘
                  │
                  ▼
┌──────────────────────────────────────────┐
│      Nginx (Reverse Proxy + SSL)         │
│      - SSL Termination                    │
│      - Load Balancing                     │
│      - Static File Serving                │
└─────────────────┬────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│ App Server 1 │    │ App Server 2 │
│  (Port 8080) │    │  (Port 8081) │
│              │    │              │
│ Docker/      │    │ Docker/      │
│ Systemd      │    │ Systemd      │
└──────┬───────┘    └──────┬───────┘
       │                   │
       └─────────┬─────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│         PostgreSQL 15 Primary            │
│         (Master Database)                │
└─────────────────┬────────────────────────┘
                  │
                  ▼ (Replication)
┌──────────────────────────────────────────┐
│     PostgreSQL 15 Replica (Optional)     │
│         (Read-Only Queries)              │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│         External Services                │
│  ┌─────────────┐  ┌─────────────┐       │
│  │   OpenAI    │  │  WhatsApp   │       │
│  │     API     │  │   Service   │       │
│  └─────────────┘  └─────────────┘       │
└──────────────────────────────────────────┘
```

## 📦 Component Breakdown

### Packages

**cmd/**
- `api/main.go` - Application entry point

**internal/** (Private packages)
- `handler/` - HTTP request handlers
- `middleware/` - HTTP middleware
- `model/` - Domain models and DTOs
- `repository/` - Data access layer
- `service/` - Business logic
- `whatsapp/` - WhatsApp integration
- `llm/` - LLM integration

**pkg/** (Public packages)
- `config/` - Configuration management
- `database/` - Database connection
- `logger/` - Logging utilities
- `security/` - Security utilities

## 🔄 Request Lifecycle

```
1. HTTP Request arrives
   └─> Nginx receives request

2. Nginx processes
   ├─> SSL termination
   ├─> Load balancing
   └─> Forward to app server

3. Chi Router
   ├─> Middleware Stack
   │   ├─> RequestID
   │   ├─> Logger
   │   ├─> Recoverer
   │   ├─> Timeout
   │   ├─> CORS
   │   └─> TenantExtractor (if tenant route)
   │
   └─> Route to Handler

4. Handler
   ├─> Validate input
   ├─> Call Service (if complex logic)
   └─> Call Repository

5. Repository
   ├─> Build SQL query
   ├─> Add tenant_id filter (from context)
   ├─> Execute query
   └─> Return results

6. Handler
   ├─> Format response
   └─> Write HTTP response

7. Response sent to client
```

## 🔍 Monitoring Points

- **Health Check:** `/health`
- **Metrics:** PostgreSQL connections, response times
- **Logs:** Structured JSON logs with slog
- **Errors:** Automatic error tracking with stack traces

## ⚡ Performance Optimizations

1. **Database:**
   - Connection pooling (25 max, 5 idle)
   - Indexed columns (tenant_id, phone lookups, searches)
   - Prepared statements (via repositories)

2. **Application:**
   - Graceful shutdown
   - Context timeouts (60s)
   - Middleware caching (tenant lookup)

3. **Static Assets:**
   - Nginx serving (if configured)
   - CDN for images (future)
   - Compressed responses

---

**Architecture Status:** ✅ Complete Foundation, Ready for Scale
