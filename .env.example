# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=autolmk
DB_PASSWORD=your_database_password_here
DB_NAME=autolmk
DB_SSLMODE=disable

# Server Configuration
PORT=8080
ENV=development

# LLM Configuration
# Options: openai, anthropic, zai
LLM_PROVIDER=zai
LLM_API_KEY=your_zai_api_key_here
LLM_MODEL=glm-4.6
# Z.AI specific endpoint (only used when LLM_PROVIDER=zai)
ZAI_ENDPOINT=https://api.z.ai/api/coding/paas/v4/chat/completions

# WhatsApp Configuration
WHATSAPP_SESSION_PATH=./whatsapp_sessions

# Security
JWT_SECRET=change-this-in-production-to-random-secure-string

# ==============================================================================
# SETUP INSTRUCTIONS:
# ==============================================================================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update the values:
#    - Set DB_PASSWORD to your PostgreSQL password
#    - Set LLM_API_KEY to your Z.AI API key (get from https://z.ai/)
#    - Change JWT_SECRET to a random secure string
#
# 3. Run database migrations:
#    make migrate-up
#
# 4. Start the server:
#    go run cmd/api/main.go
#    # or with hot reload:
#    make dev
# ==============================================================================

# ==============================================================================
# LLM PROVIDER OPTIONS:
# ==============================================================================
# Z.AI (GLM-4.6):
#   LLM_PROVIDER=zai
#   LLM_MODEL=glm-4.6
#   ZAI_ENDPOINT=https://api.z.ai/api/coding/paas/v4/chat/completions
#   Get API key from: https://z.ai/
#
# OpenAI (GPT-4):
#   LLM_PROVIDER=openai
#   LLM_MODEL=gpt-4o-mini
#   LLM_API_KEY=sk-...
#   (Note: OpenAI integration not yet implemented)
#
# Anthropic (Claude):
#   LLM_PROVIDER=anthropic
#   LLM_MODEL=claude-3-5-haiku-20241022
#   LLM_API_KEY=sk-ant-...
#   (Note: Anthropic integration not yet implemented)
# ==============================================================================
